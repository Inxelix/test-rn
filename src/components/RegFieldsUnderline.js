import styled from 'styled-components';

import {
	colors,
} from '../constants';


export const RegFieldsUnderline = styled.View`
	border-style: solid;
	border-bottom-width: 0.5;
	border-bottom-color: ${colors.thinLineColor};
`;